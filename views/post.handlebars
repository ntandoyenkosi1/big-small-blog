<div class="single-post" id={{post.id}}>

    <h2>{{post.title}}</h2>
    <span>{{post.likes}} likes</span>
    <span class="post-details">Posted on: {{format_date post.createdAt}} by {{post.User.name}}</span>
    <button id={{post.id}}>Like</button>
</div>
<h1>Comments</h1>
{{#each post.Comments}}

<div class="post">
    <div class="user">
        <span>
            <svg xmlns="http://www.w3.org/2000/svg" width="50" height="50" fill="currentColor"
                class="bi bi-person-circle" viewBox="0 0 16 16">
                <path d="M11 6a3 3 0 1 1-6 0 3 3 0 0 1 6 0z" />
                <path fill-rule="evenodd"
                    d="M0 8a8 8 0 1 1 16 0A8 8 0 0 1 0 8zm8-7a7 7 0 0 0-5.468 11.37C3.242 11.226 4.805 10 8 10s4.757 1.225 5.468 2.37A7 7 0 0 0 8 1z" />
            </svg>
        </span>
        <span>{{post.User.name}} tet</span>
    </div>
    <div class="content">
        <span>{{content}}</span>
        on <span>{{format_date createdAt}}</span>
    </div>

    <div class="like">
        <span>{{likes}} &nbsp;</span>
        <button id={{id}}>Like</button>
    </div>


</div>
{{/each}}
{{#if loggedIn}}
<input type="text" placeholder="Add comment" />
<input id="submit" data-id={{post.id}} value="Submit" type="button" />
{{/if}}
<script src="/js/post.js"></script>